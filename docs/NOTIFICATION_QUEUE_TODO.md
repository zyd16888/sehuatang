# Telegram 内存队列拆分

边界：按用户要求使用同进程内存队列和独立发送线程；MongoDB 维持采集数据职责。

- [x] 采集端只入队，Bot 延迟到消费线程初始化。
- [x] x1080x 按保存批次入队，sehuatang 资源和汇总分别入队。
- [x] 有界 FIFO、容量拒绝统计、有限重试、429 退避与分组进度。
- [x] 正常单次 CLI 清空队列；服务停止/重启有界收尾。
- [x] 通知队列 API 和管理标签，当前进程内失败重新入队。
- [x] 定向测试和界面验证完成。

限制：队列、进度和最近结果不持久化；崩溃或退出后不能自动恢复通知。
本轮验证使用模拟 Telegram，不发送真实消息，不改生产配置或数据库。

验证：131 项测试中，127 项通过，4 项需要临时 MongoDB 的集成测试跳过；
通知专项 18 项全部通过，覆盖原有 CLI 和 `main.py` 直接入口的队列收尾。
浏览器验证了桌面与 390 像素窄屏状态展示，无页面横向溢出或控制台错误。
Python/JavaScript 语法、示例 YAML、UTF-8 无 BOM 和 `git diff --check` 通过。
