# sehuatang 运行统计修复

- [x] 普通采集改为结构化板块汇总，来源运行记录聚合统一计数。
- [x] 列表去重、详情目标计数、确认保存数、分阶段失败和状态判定。
- [x] 重试补充发现数，保留已成功批次；无效目标也计入失败。
- [x] dry-run / 未启用 MongoDB 的保存数为零。
- [x] 部分批量写入失败保留明确确认的前缀计数，未完成目标进入原有失败台账。
- [x] 旧历史记录保持未知值，不从缺失数据推测零值。
- [x] 150 项离线测试通过，4 项需要临时 MongoDB 的集成测试跳过。
- [x] Python 语法、UTF-8 无 BOM 和 git diff --check 检查。

验证使用模拟数据库和页面响应，覆盖源统计、运行汇总、历史写入及读取 API。
没有采集真实站点、写生产数据库、提交或部署。
