# x1080x 限流与补抓恢复

- [x] 统一请求间隔与限流识别，覆盖直连、HTTP 重试和过盾返回。
- [x] 增量遇限流停止本轮；补抓自动递增冷却、重试原目标，不消耗失败台账次数。
- [x] 保持补抓检查点和未完成详情的恢复语义，支持退出冷却等待。
- [x] 细分详情校验错误，保留有数量和大小上限的诊断快照。
- [x] 更新配置说明，验证限流恢复、并发节流、检查点和详情诊断。

范围：x1080x 来源与必要公共合同。保留无关工作树改动；不部署、不运行生产补抓。

验证：`python -B -m tests.run_offline` 共 173 项，169 项通过，4 项 MongoDB
集成测试按离线条件跳过。覆盖连续限流退避与恢复、详情原目标重试、请求间隔、
Retry-After、增量中止、台账次数、补抓检查点、停止等待、dry-run 和诊断快照上限。
Python AST、UTF-8 无 BOM 与 `git diff --check` 通过。

未进行真实站点限流恢复、生产数据库写入或部署验证；实际冷却时长需结合站点反馈调整。
