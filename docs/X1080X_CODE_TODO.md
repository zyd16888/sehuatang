# x1080x 单字母及单数字番号识别

- [x] 标题括号与磁链 dn 支持明确分隔的单字母番号及单数字序号。
- [x] 使用用户提供的四条样本及日期、编码标记等反例验证。
- [x] 历史空 code 补录同步资源指纹与更新时间，保留首次采集时间及已有 code。
- [x] 提供 dry-run 字段差异预览，验证并发更新保护和重复执行。
- [x] 更新项目主 README，核对部署、运行命令、恢复和补录说明。

范围：识别工具、x1080x 补录脚本及必要的指纹字段共享。只做离线验证，不执行生产回填。

验证：`python -B -m tests.run_offline` 共 185 项，181 项通过、4 项本机 MongoDB
集成测试按离线条件跳过。四条样本输出为 XB-1774、M-331、JV-78、XJX-2。
离线样本 dry-run 展示缺失 code 的字段差异且写入数为 0；并发写入保护与重复执行使用模拟集合验证。
