# x1080x 番号识别与历史补录

- [x] 标题括号与磁链 dn 支持明确分隔的单字母番号及单数字序号。
- [x] 使用用户提供的四条样本及日期、编码标记等反例验证。
- [x] 历史空 code 补录同步资源指纹与更新时间，保留首次采集时间及已有 code。
- [x] 提供 dry-run 字段差异预览，验证并发更新保护和重复执行。
- [x] 更新项目主 README，核对部署、运行命令、恢复和补录说明。
- [x] 支持日期加序号、分集后缀和多段字母前缀，标题与磁链共用完整 token 识别。
- [x] 用新增 155 条编号样本验证识别与 dry-run，并保留完整后缀、前导零。

范围：识别工具、x1080x 补录脚本及必要的指纹字段共享。只做离线验证，不执行生产回填。

首轮验证：`python -B -m tests.run_offline` 共 185 项，181 项通过、4 项本机 MongoDB
集成测试按离线条件跳过。四条样本输出为 XB-1774、M-331、JV-78、XJX-2。
离线样本 dry-run 展示缺失 code 的字段差异且写入数为 0；并发写入保护与重复执行使用模拟集合验证。

多段编号验证：新增附件 155 条，修复前标题识别为 0 条；修复后原始标题与离线补录
均为识别 155、未识别 0，`sample-limit=50` 展示 50 条差异，写入 0 条。
样本包含 120 条前缀日期序号、30 条多段数字、5 条多段字母前缀。
仓库 fixture 只保存编号及预期结果；全部样本同时验证标题、磁链 dn 和完整分段。
完整离线套件共 190 项，186 项通过、4 项本机 MongoDB 集成测试跳过；
语法、UTF-8 无 BOM、文档链接和 `git diff --check` 通过。未执行生产数据库回填。
