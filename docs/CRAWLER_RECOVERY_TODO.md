# 爬虫恢复与管理页整改

- [x] 区分 HTTP 内部尝试与台账累计失败；默认五次上限，递增退避。
- [x] Mongo/JSON 使用相同停止条件，旧数据自动受上限约束。
- [x] 单条及按来源批量重新入队，保留历史次数和恢复时间。
- [x] 修复跨日期详情恢复与阶段切换后的无限重试。
- [x] R18/CF 有界转换，协调并发 Cookie 更新，不返回剩余拦截页。
- [x] x1080x 会话缓存和连接复用，删除无条件重复直连，计入过盾耗时。
- [x] 统一来源运行状态和互斥，覆盖抓取、重试和分页补抓。
- [x] 原生管理页重排为运行概览、来源卡片和功能标签，突出时间与运行状态。
- [x] 定向单元测试与临时本机 MongoDB 集成验证。
- [x] 桌面和 390 像素窄屏浏览器检查、筛选和日志交互检查。
- [x] 新版客户端少量只读验证：两个 sehuatang 帖子均取得正文，复用一次 R18 验证。

验证结果：111 项测试全部通过，其中 4 项在临时本机 MongoDB 8.0.13 执行；
Python/JavaScript 语法、UTF-8 无 BOM 与 `git diff --check` 通过。

生产验证边界：没有运行整站采集、修改生产台账、部署或重启服务。
x1080x 的实际提速与生产环境反复 CF 挑战是否消失仍需部署后对照日志确认。
