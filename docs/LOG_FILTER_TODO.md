# 日志级别与模块筛选

- [x] 新日志带固定模块标记；来源公共 HTTP/引擎显式归属，旧日志保留未分类。
- [x] 日志接口按级别和模块从文件尾部筛选，限定扫描量并保留完整异常堆栈。
- [x] 页面增加级别、模块选择，保留自动刷新及阅读时滚动位置，说明筛选和扫描范围。
- [x] 验证模块归属、组合筛选、旧日志、堆栈、扫描上限与页面交互。

沿用 crawler.log/error.log 及现有轮转；不迁移数据库。保留已完成的分页补抓改动。

验证：`python -B -m tests.run_offline` 共 222 项，218 项通过，4 项本机 MongoDB
集成测试按离线条件跳过。覆盖调用组件归属、并发绑定、HTTP 来源、历史未分类、
组合筛选、较早匹配记录、完整堆栈、跨读取块的 UTF-8 长日志及扫描上限。
本地模拟数据的浏览器验证覆盖组合筛选、堆栈、无结果提示、375px 窄屏及自动刷新。
Python/JavaScript 语法、UTF-8 无 BOM 与 `git diff --check` 已检查。未部署或读取生产日志。
