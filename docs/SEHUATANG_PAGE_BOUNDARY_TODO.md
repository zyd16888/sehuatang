# Sehuatang 分页补抓边界

- [x] 从末页链接、总页数提示和明确的最后一页状态识别边界，不把邻近页码当总页数。
- [x] 分页补抓和续跑使用可靠边界，识别页码错位与整页重复，不推进异常页检查点。
- [x] 严格区分正常空列表、提示/验证页面和解析失败。
- [x] 覆盖多分区、无分页栏、续跑越界、合法空页、异常页和 dry-run 回归。
- [x] 更新主 README 的分页停止与恢复说明。

不自动重写旧检查点；不改动 x1080x 或年度补抓策略。只进行离线验证。

验证：`python -B -m tests.run_offline` 共 208 项，204 项通过，4 项本机 MongoDB
集成测试按离线条件跳过。包含检查点 1000 / 实际末页 50 的续跑场景：只请求第一页，
不请求 1001 及后续页；旧检查点保留 1000。本轮汇总和日志输出确认的末页。
多分区编排、异常页不推进、正常部分重叠、已存在帖子及 dry-run 均已离线验证。
Python 语法、UTF-8 无 BOM 和 `git diff --check` 通过；未验证真实站点响应或部署。
